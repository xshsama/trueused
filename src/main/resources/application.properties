spring.application.name=trueused

# JWT settings
# 至少 256 位（建议使用 Base64 编码）。生产环境务必覆盖。
security.jwt.secret=76zE0lFT4+aY7fcErR/Gc0lAg715f1mS8GV5SaQyBWw=
# 过期时间（毫秒），默认一天
security.jwt.expiration-ms=86400000

# MySQL 示例
spring.datasource.url=jdbc:mysql://localhost:1100/trueused?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=ying200568
# 修正拼写：hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# Flyway 基础配置（先接管，后完善 V1 脚本）
# 恢复 Flyway 以执行迁移（已升级 Flyway 版本并引入 MySQL 模块）
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.clean-disabled=true
# 现有库直接接管：首次迁移时基线为 1（后续补充 V1__init.sql 可再关闭）
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1
spring.flyway.baseline-description=Initial baseline


# Cloudinary settings
cloudinary.cloud_name=dhgbkfhwo
cloudinary.api_key=842418411579682
cloudinary.api_secret=oSgGsuRV1j9d2gVmcGKkqLdjWCQ
